http:
  routers:
    root-dashboard:
      rule: Host(`alshawwaf.ca`)
      service: root-dashboard
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
  services:
    root-dashboard:
      loadBalancer:
        servers:
          - url: http://dokploy:3000
